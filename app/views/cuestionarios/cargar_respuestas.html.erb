<%= form_tag(  :method=>'post', :multipart => true) do %>
<div class='bs-example form-horizontal'>
  <div class="row">
    <div class="col-lg-10">
       <div class="well">
          <fieldset>
            <% @cuestionario.preguntas.each do |pregunta| %>
              <div class="form-group">
              <% if pregunta.tipo == 'Texto Libre'%>
                <label for="inputTitle" class="col-lg-3 control-label"><%= pregunta.texto %></label>
                  <% @respuestas.each do |respuesta|%>
                    <% if  pregunta.id == respuesta.pregunta_id%>
                      <div class="col-lg-9">    
                        <%= text_area_tag :texto, respuesta.respuesta.to_s  ,:disabled => true%>
                         &nbsp;&nbsp;&nbsp;&nbsp;<%= text_area_tag :comentario, nil  %>
                    <% end %>
                  <% end %> 
                  </div>
                   <% end %>
                </div>
              </div>
           
            <% end %>
            <%= hidden_field_tag :id_cuestionario, @cuestionario.id %>
            <%= hidden_field_tag :id_usuario, current_user.id %>
            <div class="form-group">
              <div class="col-lg-10 col-lg-offset-10">
                <input type="submit" value="Aceptar" class="btn btn-primary">
              </div>
            </div>
        </fieldset>
      </div>
    </div>
   </div>
  </div>
<% end %>